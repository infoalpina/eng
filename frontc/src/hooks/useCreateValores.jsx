/*import { useState, useEffect } from 'react'
import encaminharPara from 'functions/useEncaminhar'

const useCreateValores = (permissioncadastro) => {
  useEffect(() => {
    // Verificar se permissioncadastro não está vazio
    if (permissioncadastro !== '') {
      // Encaminhar para a rota desejada
      encaminharPara(`../TorreCadastradaComSucesso/${permissioncadastro}`)
    }
  }, [permissioncadastro])
}

export default useCreateValores*/
